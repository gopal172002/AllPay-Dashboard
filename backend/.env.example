PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/allpay_db
JWT_SECRET=change-me-in-production

# Shared secret for AllpayEmployeeApp (header X-AllPay-Sync-Secret).
# If unset, mobile sync is allowed without a secret (local dev only).
MOBILE_SYNC_SECRET=
